import React from 'react';
import styles from './TablesEvents.module.scss';

const TablesEvents = (/*{title}*/) => (
  <div className={styles.component}>
    <h2>TablesEvents view</h2>
  </div>
);

/*Login.propTypes = {
  title: PropTypes.string,
};
*/
export default TablesEvents;
