import React from 'react';
import styles from './WaiterOrder.module.scss';

const WaiterOrder = (/*{title}*/) => (
  <div className={styles.component}>
    <h2>WaiterOrder view</h2>
  </div>
);

/*Login.propTypes = {
  title: PropTypes.string,
};
*/
export default WaiterOrder;
